{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "prepare": "nuxi prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "nuxt-svgo": "^1.1.0"
  },
  "dependencies": {
    "nuxt-swiper": "^0.1.6"
  }
}
